// beforeEach(() => {
//     //Insert test data?
// });

// AfterEach(() => {
//     // Cleanse db
// });

// describe('User controller tests', () => {
//     it('getUserRecords, should returned unaltered test data', async done => {
//     })

//     it('updateUserRecord, update record then call getUserRecords', async done => {
//     })
    
//     it('updateUserRecord, update firstname only, correctly', async done => {
//     })

//     it('updateUserRecord, update firstname only, incorrectly, should flag', async done => {
//     })
    
//     it('updateUserRecord, update surname only, correctly', async done => {
//     })

//     it('updateUserRecord, update surname only, incorrectly, should flag', async done => {
//     })
    
//     it('updateUserRecord, update date of birth only, correctly', async done => {
//     })

//     it('updateUserRecord, update date of birth only, incorrectly, should flag', async done => {
//     }) 
    
//     it('updateUserRecord, update membership and start date only, correctly', async done => {
//     })

//     it('updateUserRecord, update membership and start date only, incorrectly, should flag', async done => {
//     })
// })